# Install all dependencies including dev during build
# This ensures @tailwindcss/postcss is available for next build
fund=false
audit=false
# Skip downloading Electron desktop binary during npm install on headless/CI servers like Render
ELECTRON_SKIP_BINARY_DOWNLOAD=1
electron_skip_binary_download=1

